services:
  - type: web
    name: choruschat
    env: node
    plan: free
    buildCommand: npm install
    startCommand: npm start
    autoDeploy: true
    envVars:
      - key: NODE_ENV
        value: production
      - key: SESSION_SECRET
        generateValue: true
      - key: STORAGE_DIR
        value: /var/data
    disk:
      name: chorus-data
      mountPath: /var/data
      sizeGB: 1
